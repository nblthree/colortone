{
  "version": 2,
  "name": "colortone",
  "builds": [
    {
      "src": "/demo/package.json",
      "use": "@now/static-build",
      "config": {"distDir": "build"}
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "demo/$1" }
  ]
}